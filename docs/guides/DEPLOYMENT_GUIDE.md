# 部署指南

## 🌟 部署指南

### 1. 介绍

本指南详细介绍了AI共创平台的部署方式，包括多种数据库配置和部署环境。您可以根据自己的需求选择合适的部署方式。

### 2. 目录

```
- [1. 介绍](#1-介绍)
- [2. 目录](#2-目录)
- [3. 部署方式对比](#3-部署方式对比)
  - [3.1 数据库配置对比](#31-数据库配置对比)
  - [3.2 部署平台对比](#32-部署平台对比)
- [4. 环境准备](#4-环境准备)
- [5. 数据库配置](#5-数据库配置)
  - [5.1 LocalStorage（开发环境）](#51-localstorage-开发环境)
  - [5.2 直接PostgreSQL连接](#52-直接postgresql连接)
  - [5.3 Neon Data API](#53-neon-data-api)
- [6. 部署步骤](#6-部署步骤)
  - [6.1 Vercel部署](#61-vercel部署)
  - [6.2 Netlify部署](#62-netlify部署)
  - [6.3 GitHub Pages部署](#63-github-pages部署)
  - [6.4 自建服务器部署](#64-自建服务器部署)
  - [6.5 Docker部署](#65-docker部署)
- [7. 环境变量配置](#7-环境变量配置)
- [8. CI/CD配置](#8-cicd配置)
- [9. 故障排除](#9-故障排除)
- [10. 常见问题](#10-常见问题)
- [11. 总结](#11-总结)
```

### 3. 部署方式对比

#### 3.1 数据库配置对比

| 数据库配置 | 特点 | 适用场景 |
|------------|------|----------|
| **LocalStorage** | 无需外部数据库，适合开发和测试 | 开发环境，本地测试 |
| **直接PostgreSQL连接** | 配置简单，性能优秀，成熟稳定 | 中小型项目，企业级应用 |
| **Neon Data API** | Serverless兼容，API密钥认证，无需维护连接池 | 无服务器架构，动态扩展需求 |

#### 3.2 部署平台对比

| 部署平台 | 特点 | 适用场景 |
|----------|------|----------|
| **Vercel** | 快速部署，Serverless支持，自动CI/CD | 个人和小团队快速部署 |
| **Netlify** | 持续部署，CDN加速，表单处理 | 中小型项目 |
| **GitHub Pages** | 免费，适合开源项目，自动部署 | 开源项目，静态网站 |
| **自建服务器** | 完全可控，高性能，自定义配置 | 大型项目，企业级应用 |
| **Docker部署** | 容器化，易于扩展，环境一致 | 微服务架构，DevOps环境 |

### 4. 环境准备

在部署之前，确保您已经完成以下准备工作：

1. 克隆项目代码到本地
2. 安装Node.js 18+和包管理工具（PNPM推荐）
3. 创建GitHub/GitLab仓库
4. 根据选择的数据库配置，准备相应的数据库环境
5. 注册相关部署平台账号（如果使用云部署）

### 5. 数据库配置

#### 5.1 LocalStorage（开发环境）

LocalStorage是默认的数据库配置，无需额外设置。该配置仅适用于开发和测试环境，不建议用于生产环境。

**环境变量配置**：
```env
DB_TYPE="local"
```

#### 5.2 直接PostgreSQL连接

直接PostgreSQL连接是一种成熟稳定的数据库配置方式，适合大多数生产环境。

**步骤1：获取PostgreSQL连接字符串**

1. 登录Neon控制台或其他PostgreSQL服务提供商
2. 进入项目页面
3. 点击左侧菜单的"连接"或"Connect"
4. 选择"psql"或"Direct Connection"
5. 复制完整的连接字符串，格式类似：
   ```
   postgresql://neondb_owner:your_password@ep-bold-flower-agmuls0b-pooler.eu-central-1.aws.neon.tech/neondb?sslmode=require
   ```

**步骤2：配置环境变量**

```env
DB_TYPE="postgresql"
DATABASE_URL="您的PostgreSQL连接字符串"
```

#### 5.3 Neon Data API

Neon Data API是一种REST API方式的数据库访问，适合Serverless架构。

**步骤1：获取Data API配置**

1. 在Neon控制台左侧菜单中，点击 **Data API**
2. 如果还未启用，点击 **Enable Data API** 按钮
3. 启用后，您将看到：
   - **API Endpoint**: 类似 `https://ep-bold-flower-agmuls0b.apirest.c-2.eu-central-1.aws.neon.tech/neondb/rest/v1`
   - **API Key**: 点击 "Generate API Key" 创建密钥，妥善保存
   - **Database Name**: 默认为 `neondb`

**步骤2：配置环境变量**

```env
DB_TYPE="neon_api"
NEON_API_ENDPOINT="您的Data API Endpoint"
NEON_API_KEY="您的API Key"
NEON_DB_NAME="neondb"
```

### 6. 部署步骤

#### 6.1 Vercel部署

Vercel是一个流行的Serverless部署平台，适合快速部署React应用。

**步骤1：连接GitHub仓库**

1. 登录Vercel控制台
2. 点击"Add New Project"
3. 选择"Import Git Repository"
4. 连接您的GitHub仓库
5. 点击"Import"

**步骤2：配置项目**

1. 选择项目名称和团队
2. 配置环境变量（参考[环境变量配置](#7-环境变量配置)）
3. 点击"Deploy"

**步骤3：验证部署**

1. 部署完成后，访问Vercel提供的域名
2. 测试注册功能，确保数据能正确存储到数据库
3. 测试登录功能，确保能正确读取用户数据
4. 访问管理后台，查看用户列表

#### 6.2 Netlify部署

Netlify是另一个流行的静态网站部署平台，提供持续部署和CDN加速。

**步骤1：连接GitHub仓库**

1. 登录Netlify控制台
2. 点击"Add new site"
3. 选择"Import an existing project"
4. 连接您的GitHub仓库

**步骤2：配置项目**

1. 配置构建设置：
   - **Build command**: `pnpm build` 或 `npm run build`
   - **Publish directory**: `dist`
2. 配置环境变量（参考[环境变量配置](#7-环境变量配置)）
3. 点击"Deploy site"

**步骤3：验证部署**

1. 部署完成后，访问Netlify提供的域名
2. 测试核心功能，确保部署成功

#### 6.3 GitHub Pages部署

GitHub Pages适合开源项目，提供免费的静态网站托管服务。

**步骤1：配置GitHub Pages**

1. 进入GitHub仓库设置
2. 选择"Pages"选项卡
3. 配置"Build and deployment"：
   - **Source**: "GitHub Actions"
   - 选择合适的GitHub Actions工作流模板

**步骤2：创建部署配置**

创建 `.github/workflows/deploy.yml` 文件：

```yaml
name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    
    - name: Setup Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '18'
        cache: 'pnpm'
    
    - name: Install dependencies
      run: pnpm install
    
    - name: Build
      run: pnpm build
    
    - name: Deploy
      uses: peaceiris/actions-gh-pages@v3
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        publish_dir: ./dist
```

**步骤3：验证部署**

1. 推送代码到GitHub仓库
2. 等待GitHub Actions完成部署
3. 访问 `https://<username>.github.io/<repository-name>`

#### 6.4 自建服务器部署

自建服务器适合需要完全控制部署环境的项目。

**步骤1：准备服务器**

1. 购买或配置一台服务器（推荐Ubuntu 20.04+）
2. 安装Node.js 18+、Nginx、Git等必要软件
3. 配置防火墙规则，开放80和443端口

**步骤2：部署应用**

1. 克隆项目代码到服务器
2. 安装依赖：`pnpm install`
3. 构建生产版本：`pnpm build`
4. 配置Nginx虚拟主机：

```nginx
server {
    listen 80;
    server_name your-domain.com;
    
    location / {
        root /path/to/your/project/dist;
        index index.html;
        try_files $uri $uri/ /index.html;
    }
}
```

5. 启用HTTPS（推荐使用Let's Encrypt）：

```bash
sudo certbot --nginx -d your-domain.com
```

**步骤3：验证部署**

1. 重启Nginx：`sudo systemctl restart nginx`
2. 访问您的域名，测试核心功能

#### 6.5 Docker部署

Docker部署适合微服务架构和DevOps环境，提供一致的部署环境。

**步骤1：创建Dockerfile**

```dockerfile
# 使用Node.js 18作为基础镜像
FROM node:18-alpine as build

# 设置工作目录
WORKDIR /app

# 复制package.json和pnpm-lock.yaml
COPY package.json pnpm-lock.yaml ./

# 安装依赖
RUN npm install -g pnpm && pnpm install

# 复制源代码
COPY . .

# 构建生产版本
RUN pnpm build

# 使用Nginx作为生产服务器
FROM nginx:alpine

# 复制构建产物到Nginx静态目录
COPY --from=build /app/dist /usr/share/nginx/html

# 复制Nginx配置
COPY nginx.conf /etc/nginx/conf.d/default.conf

# 暴露80端口
EXPOSE 80

# 启动Nginx
CMD ["nginx", "-g", "daemon off;"]
```

**步骤2：创建Nginx配置文件**

创建 `nginx.conf` 文件：

```nginx
server {
    listen 80;
    server_name localhost;
    
    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri $uri/ /index.html;
    }
}
```

**步骤3：构建和运行Docker容器**

```bash
# 构建Docker镜像
docker build -t ai-co-creation-platform .

# 运行Docker容器
docker run -p 80:80 ai-co-creation-platform
```

**步骤4：验证部署**

访问 `http://localhost`，测试核心功能。

### 7. 环境变量配置

以下是完整的环境变量配置表：

| 变量名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| **DB_TYPE** | 字符串 | "local" | 数据库类型：local, postgresql, neon_api |
| **DATABASE_URL** | 字符串 | - | PostgreSQL连接字符串（当DB_TYPE=postgresql时必填） |
| **NEON_API_ENDPOINT** | 字符串 | - | Neon Data API端点（当DB_TYPE=neon_api时必填） |
| **NEON_API_KEY** | 字符串 | - | Neon Data API密钥（当DB_TYPE=neon_api时必填） |
| **NEON_DB_NAME** | 字符串 | "neondb" | Neon数据库名称（当DB_TYPE=neon_api时使用） |
| **JWT_SECRET** | 字符串 | - | JWT签名密钥，用于用户认证 |
| **JWT_EXPIRES_IN** | 字符串 | "7d" | JWT过期时间 |
| **CORS_ALLOW_ORIGIN** | 字符串 | "*" | 允许的跨域来源 |

### 8. CI/CD配置

#### 8.1 GitHub Actions配置

创建 `.github/workflows/deploy.yml` 文件，配置自动构建和部署流程：

```yaml
name: Deploy

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
          cache: 'pnpm'
      - name: Install dependencies
        run: pnpm install
      - name: Build
        run: pnpm build
      - name: Lint
        run: pnpm lint
      - name: Test
        run: pnpm test

  deploy:
    needs: build
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    steps:
      # 添加您的部署步骤，例如Vercel部署、Netlify部署等
```

#### 8.2 GitLab CI配置

创建 `.gitlab-ci.yml` 文件：

```yaml
image: node:18-alpine

stages:
  - install
  - build
  - test
  - deploy

install_dependencies:
  stage: install
  script:
    - npm install -g pnpm
    - pnpm install
  artifacts:
    paths:
      - node_modules/

build:
  stage: build
  script:
    - pnpm build
  artifacts:
    paths:
      - dist/

lint:
  stage: test
  script:
    - pnpm lint

test:
  stage: test
  script:
    - pnpm test

pages:
  stage: deploy
  script:
    - cp -r dist/* public/
  artifacts:
    paths:
      - public/
  only:
    - main
```

### 9. 故障排除

#### 9.1 数据库连接问题

**问题**：无法连接到数据库

**解决方案**：
- 检查数据库服务是否正常运行
- 检查数据库连接字符串是否正确
- 检查数据库用户权限是否足够
- 检查网络连接是否畅通
- 检查防火墙规则是否允许连接

#### 9.2 部署失败

**问题**：Vercel/Netlify部署失败

**解决方案**：
- 查看部署日志，查找具体错误信息
- 检查环境变量配置是否正确
- 检查依赖安装是否成功
- 检查构建命令是否正确

#### 9.3 注册/登录功能失败

**问题**：用户无法注册或登录

**解决方案**：
- 检查JWT_SECRET是否配置正确
- 检查数据库表结构是否完整
- 检查API端点是否返回正确的响应
- 检查浏览器控制台是否有错误信息

#### 9.4 静态资源加载失败

**问题**：CSS、JS等静态资源无法加载

**解决方案**：
- 检查构建产物是否完整
- 检查Nginx配置是否正确
- 检查CDN配置是否正确
- 检查浏览器控制台的网络请求

### 10. 常见问题

| 问题 | 解决方案 |
|------|----------|
| 如何选择合适的部署方式？ | 根据项目规模、团队经验和需求选择，推荐小型项目使用Vercel或Netlify，大型项目使用自建服务器或Docker部署 |
| 如何迁移数据到新的数据库？ | 导出旧数据库数据，在新数据库中导入数据，确保表结构一致 |
| 如何配置HTTPS？ | 推荐使用Let's Encrypt获取免费SSL证书，或使用云服务商提供的HTTPS服务 |
| 如何监控部署后的应用？ | 使用Vercel/Netlify的监控功能，或集成第三方监控服务如Sentry、Datadog等 |
| 如何回滚部署？ | 在Vercel/Netlify控制台选择历史部署版本，点击"Rollback"按钮 |

### 11. 总结

本指南详细介绍了AI共创平台的多种部署方式，包括LocalStorage、直接PostgreSQL连接和Neon Data API，以及Vercel、Netlify、GitHub Pages、自建服务器和Docker部署。您可以根据自己的需求选择合适的部署方式。

部署完成后，建议：
1. 测试核心功能，确保部署成功
2. 配置监控和日志系统，便于后续维护
3. 定期更新依赖和安全补丁
4. 建立完善的备份策略

希望本指南能帮助您顺利部署AI共创平台！如果您遇到任何问题，请参考[常见问题](#10-常见问题)或联系我们的技术支持团队。

---

## 更新日志

- **2024-01-01**：初始版本发布
- **2024-02-15**：添加Docker部署方式
- **2024-03-30**：优化故障排除部分，添加更多常见问题
- **2024-04-15**：整合多种部署文档，统一文档结构

---

## 参考资料

1. [Vercel 官方文档](https://vercel.com/docs)
2. [Netlify 官方文档](https://docs.netlify.com/)
3. [GitHub Pages 官方文档](https://docs.github.com/en/pages)
4. [Neon 官方文档](https://neon.tech/docs)
5. [Docker 官方文档](https://docs.docker.com/)
6. [Nginx 官方文档](https://nginx.org/en/docs/)
